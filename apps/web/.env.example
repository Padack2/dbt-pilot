# Neon PostgreSQL — 읽기 전용 롤 (대시보드 조회 전용)
# 실제 값은 .env.local로 복사해서 채울 것 (Next.js가 .env.local을 로드함)
DATABASE_URL_READONLY=

# Neon PostgreSQL — 쓰기 롤 (수동 REFRESH 트리거 Server Action 전용, 그 외 용도로 사용 금지)
DATABASE_URL_BATCH=
